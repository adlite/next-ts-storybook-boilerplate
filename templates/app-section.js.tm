import style from './style.module.scss';

function ${TM:COMPONENT_NAME}() {
  return (
    <div className={style.${TM:COMPONENT_NAME}}>
      ${TM:COMPONENT_NAME} component
    </div>
  );
}

export default ${TM:COMPONENT_NAME};
